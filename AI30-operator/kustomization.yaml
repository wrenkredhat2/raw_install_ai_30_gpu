apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# These resources will be applied to the cluster
resources:
  - namespace.yaml
  - operator-group.yaml
  - subscription.yaml

# Optional: You can force all resources into this namespace 
# (though it's already defined in the files above)
namespace: redhat-ods-operator
